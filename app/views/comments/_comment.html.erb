<div class="event">
  <h4>
    <%= link_to(comment.user.name, user_path(comment.user)) %> commented <%= time_ago_or_exact_date(comment.created_at) %>
  </h4>
  <p><%= truncate(comment.comment, length: 250) %></p>
</div>