<h4>
  <%= link_to(pull.user.name, user_path(pull.user)) %> sent a pull request
  <%= time_ago_or_exact_date(pull.requested_at) %>
</h4>
<h3><%= pull.title %></h3>
<p><%= truncate(pull.body, length: 250) %></p>
<hr />